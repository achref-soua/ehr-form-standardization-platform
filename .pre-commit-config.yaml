repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.5
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks working-tree scan
        entry: zricethezav/gitleaks:v8.30.1 dir . --redact --verbose
        language: docker_image
        pass_filenames: false
        always_run: true
      - id: commitlint
        name: commitlint
        entry: pnpm exec commitlint --edit
        language: system
        stages: [commit-msg]
        pass_filenames: false
